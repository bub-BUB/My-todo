Its a to-do list project with basic funcionality:
*you can add tasks, which will be displayed in a list of tasks.
*the list with tasks can be minimized or deleted.
*the page tries to adapt to different screen sizes.
*rest is experiments with styling
